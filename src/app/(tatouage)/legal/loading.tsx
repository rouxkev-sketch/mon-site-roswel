import { RideauDePageTexte } from "@/components/RideauDePageTexte";

/**
 * nº 811 — « Legal » RÉPOND AU CLIC : le rideau des pages de texte
 * (barre réelle, corps vide, pas de squelette) — voir
 * `RideauDePageTexte`.
 */
export default function ChargementLegal() {
  return <RideauDePageTexte />;
}
