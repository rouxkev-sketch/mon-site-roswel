import { RideauDePageTexte } from "@/components/RideauDePageTexte";

/**
 * nº 814 — « Terms of Use » RÉPOND AU CLIC : le rideau des pages de
 * texte (barre réelle, corps vide, pas de squelette) — voir
 * `RideauDePageTexte`, et les trois autres pages éditoriales (nº 811).
 */
export default function ChargementTerms() {
  return <RideauDePageTexte />;
}
